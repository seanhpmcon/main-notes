#Docker
When connecting to the docker container, use the WSL IP address as the host. [[Connection issues]]

Also, when creating the Postgres container, set the proper username and password to be used for connection.