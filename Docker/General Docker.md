### Working with directories in Docker
Docker can only work on directories that are two levels down from the base, for example /var/www but not /var/www/html.

### Running a container in the background
to run a container in the background, using the command `-d`. 