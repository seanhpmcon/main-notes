The `cd` command is used to change directories in windows.

### Changing Directories to a network drive
When changing directories to a network drive, use the `/d` tag to switch the drives as well. eg `cd /d U:`