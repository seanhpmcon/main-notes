#JavaScript

###### How to output a PHP array to a JavaScript variable
```js
const revenue = <?php echo json_encode($revenue); ?>;
```

In the above code, the PHP array is converted to JSON and then the echo function is used to pass it to the JavaScript variable.