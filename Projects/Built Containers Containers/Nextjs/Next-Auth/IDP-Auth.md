#nextjs #autrhentication #nextjs #keycloak 
### Source Code Local
Ubuntu: /nextjs/nextjs-auth/idp-auth

### GitHub

### Description
Show how to setup Next-auth with keycloak as the login IDP. Also shows how to properly log a user out from next-auth and Keycloak (By calling the correct logout API with the correct Parameters) since you need to log both out to kill the session.

### Requirements
Keycloak