Use the Command `git branch <create-branch-name>` to create a new git branch.

### Switch Branches
Use the `git branch` command to see the list of branches and the current branch.
Then use `git checkout <branch-name>` or `git switch <branch-name>` to switch to another branch.